import { Vector } from "./geometry/vector";

export const K = +((4 / 3) * (Math.sqrt(2) - 1)).toFixed(5);
export const anchorRadius = 2;
export const vertexWidth = 6;
export const EPSILON = 1e-8;
export const normal = new Vector(1, 0);
